import { PrivateVaultClient } from "@/components/vault/PrivateVaultClient";

export const metadata = {
  title: "Private Vault"
};

export default function VaultPage() {
  return <PrivateVaultClient />;
}
